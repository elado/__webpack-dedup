{
  "name": "toolkit",
  "main": "lib/index.js",
  "engines": {
    "node": ">=0.12.0"
  },
  "scripts": {
    "compile": "node_modules/.bin/rimraf lib && node_modules/.bin/babel -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "devDependencies": {
    "babel": "^5.0.0",
    "babel-core": "^5.0.0",
    "babel-runtime": "^5.0.0",
    "rimraf": "^2.4.3"
  },
  "dependencies": {
    "react": "^0.14.2"
  }
}
